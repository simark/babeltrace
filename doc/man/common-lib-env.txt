// SPDX-FileCopyrightText: 2017-2019 Philippe Proulx <pproulx@efficios.com>
//
// SPDX-License-Identifier: CC-BY-SA-4.0

=== Babeltrace{nbsp}2 library

include::common-common-env.txt[]

`BABELTRACE_PLUGIN_PATH`=_PATHS_::
    Set the list of directories, in order, in which dynamic plugins can
    be found before other directories are considered to _PATHS_
    (colon-separated, or semicolon on Windows).

`BABELTRACE_PLUGIN_PROVIDER_PATH`=_PATHS_::
    Set the list of directories, in order, in which dynamic plugin
    providers can be found before other directories are considered to
    _PATHS_ (colon-separated, or semicolon on Windows).

`LIBBABELTRACE2_DISABLE_PYTHON_PLUGINS`=`1`::
    Disable the loading of any Babeltrace{nbsp}2 Python plugin.

`LIBBABELTRACE2_DISABLE_STD_PLUGIN_PROVIDER_DIRS`=`1`::
    Disable the loading of plugin providers from the standard
    directories, that is, the user plugin provider directory
    (`$HOME/.local/lib/babeltrace2/plugin-providers`) and the system
    plugin provider directory.
+
Plugin providers found through the `BABELTRACE_PLUGIN_PROVIDER_PATH`
environment variable as well as built-in (static) plugin providers are
still loaded.

`LIBBABELTRACE2_DISABLE_USER_AND_SYSTEM_PLUGIN_PATHS`=`1`::
    Disable the loading of plugins and plugin providers from the user
    (typically `~/.local/lib/babeltrace2/plugins` and
    `~/.local/lib/babeltrace2/plugin-providers`) and system (for
    example `/usr/lib/babeltrace2/plugins` and
    `/usr/lib/babeltrace2/plugin-providers`) directories.
+
The directories listed in `BABELTRACE_PLUGIN_PATH` and
`BABELTRACE_PLUGIN_PROVIDER_PATH`, as well as built-in (static)
plugins and plugin providers, are still loaded.

`LIBBABELTRACE2_FORCE_COMPONENT_LOG_LEVEL`=_SPEC_::
    Force the log level of all or specific components added to any
    trace processing graph.
+
_SPEC_ is one of:
+
--
* A single log level value _LVL_.
+
In this case, libbabeltrace2 overrides the log level specified when
adding _any_ component to any trace processing graph.

* A semicolon-delimited list of __PAT__:__LVL__ specifiers.
+
__PAT__ is a globbing pattern where only the wildcard
character, “`+*+`”, is special: it matches zero or more characters.
You must escape the “`+*+`”, “`?`”, “`[`”, and “`\`” characters
with “`\`”.
+
__LVL__ is a log level value.
+
In this case, libbabeltrace2 overrides the log level specified when
adding a component of which the name matches __PAT__ to any trace
processing graph.
--
+
libbabeltrace2 dynamically checks this environment variable before
adding a component to a trace processing graph, meaning you can modify
it at any time with man:setenv[3].
+
The available values for _LVL_ are:
+
--
include::common-log-levels.txt[]
--
+
Example:
+
----
*ctf*:I;my-sink:D;*:W
----

`LIBBABELTRACE2_INIT_LOG_LEVEL`=_LVL_::
    Force the initial log level of the Babeltrace{nbsp}2 library to be _LVL_.
+
If this environment variable is set, then it overrides the log level set
by the manopt:babeltrace2[1,--log-level] option for the Babeltrace{nbsp}2
library logger.
+
The available values for _LVL_ are:
+
--
include::common-log-levels.txt[]
--

`LIBBABELTRACE2_NO_DLCLOSE`=`1`::
    Make the Babeltrace{nbsp}2 library leave any dynamically loaded
    modules (plugins and plugin providers) open at exit. This can be
    useful for debugging purposes.


=== Babeltrace{nbsp}2 Python bindings

`BABELTRACE_PYTHON_BT2_LOG_LEVEL`=_LVL_::
    Force the Babeltrace{nbsp}2 Python bindings log level to be _LVL_.
+
If this environment variable is set, then it overrides the log level set
by the manopt:babeltrace2[1,--log-level] option for the Python bindings
logger.
+
The available values for _LVL_ are:
+
--
include::common-log-levels.txt[]
--
